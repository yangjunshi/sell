# sell

> 本项目是基于vue2实战项目，vue2 +vue-router2 + es6 +webpack 高仿饿了么app


``` bash
# 安装依赖
npm install

# 启动本地开发环境服务（localhost:8080）
npm run dev

# 打包发布代码
npm run build

# 运行服务（localhost:9000）
node prod.server.js
```

